{% extends "base.html" %}
{% block pagetitle %}Assets{% endblock %}
{% block content %}
{% if documents %}
<div class="list-group">
    {% for document in documents %}
    <div class="list-group-item">
      <h4 class="list-group-item-heading">{{ document.title }}</h4>
      {% if document.language %}
      <div>{{ document.language }}</div>
      <a href="{% url "converted" document.doc_id %}" class="list-group-item-text">text</a> | 
      <a href="{{ document.docfile.url }}" class="list-group-item-text">file</a> |
      <a href="{% url "mark_words" document.doc_id %}" class="list-group-item-text">words</a>
      <a href="{{ url "micr/Frequency_list" }}" class="list-group-item-text">Frequency List</a>
      {% else %}
      Document is being processed. Please wait
      {% endif %}
    </div>
    {% endfor %}
</div>
{% else %}
<p>No assets.</p>
{% endif %}
<p><a href='{% url "upload" %}'>Upload?</a></p>
{% endblock %}
